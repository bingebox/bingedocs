
## 1. cluade在已有Java系统上做增改编程的能力表现

1）新增的方法里流程处理还不错；
2）DTO类位置放错，导致低层级工程无法编译，说明它没有分析语法错误； -- 用后hook钩子调用mvn build
3）对一个现有方法加了一个isCopy参数，代码修改完全错误：
没有理解代码意图，也可能我没有说明isCopy的任何中文含义。 -- 只能更详细一点描述
4）现有方法加了isCopy参数，3处调用点有2处没有增加设置参数值，编译错误也无感知。

## 2.  即使在~/.claude/CLAUDE.md里写明了 

```
 **不得擅自操作**：没有明确指示我执行某个操作时，不要执行; 没有明确让我添加、修改或删除某段代码时，不要擅自操作，即使认为有 bug 或有必要，也要先提出建议，等待确认后再执行 。 
 没有明确指示我执行git commit或push，不要执行。
```
但是在/ecc:plan确认之后，让它执行编码，它还是编码完成之后直接git add/git commit/git push, 应该是LLM执行问题，Agent只是做足够的prompt规则提醒。
这个很讨厌，让我失去了/diff的机会，只有去gitlab界面上看diff。-- 用后hook钩子拦截, 待人工确认
